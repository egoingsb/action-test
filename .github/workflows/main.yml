name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    #- uses: actions/checkout@v2
    - name: git setting
      run : git clone git@github.com:egoingsb/action-test.git .
    - name: Run pwd
      run: pwd
    - name: Run ls -al
      run: ls -al
